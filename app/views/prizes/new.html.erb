
<%= form_with scope: :prize, url: prizes_path, local: true do |form| %>

<% if @prize.errors.any? %>
<div id="error explanation">
  <h2>
    <% pluralize(@prize.errors.count, "error") %> prohibited this article from being saved:
    </h2>
    <ul>
      <% @prize.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
      <% end %>
      </ul>
      </div>
      <% end %>
      </ul>
      </div>
  <p>
    <%= form.label :title %><br>
    <%= form.text_field :title %>
  </p>

  <p>
    <%= form.label :text %><br>
    <%= form.text_area :text %>
  </p>

  <p>
    <%= form.submit %>
  </p>

  <%= link_to 'Back', prizes_path %>

<% end %>
