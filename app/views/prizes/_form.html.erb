<%= form_with model: @prize, local: true do |form| %>

  <% if @prize.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@prize.errors.count, "error") %> prohibited
        this article from being saved:
      </h2>
      <ul>
        <% @prize.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= form.label :title %><br>
    <%= form.text_field :title %>
  </p>

  <p>
    <%= form.label :price %><br>
    <%= form.text_area :price %>
  </p>

  <p>
    <%= form.label :prizeimage %><br>
    <%= form.file_field :prizeimage %>
  </p>

  <p>
    <%= form.submit %>
  </p>

<% end %>
